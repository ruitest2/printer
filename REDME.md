# printer

```
> just --list
Available recipes:
    bootstrap # Install uv and cups using Homebrew
    install   # Install python dependencies
    print     # Print a file using the CUPS helper script
```
